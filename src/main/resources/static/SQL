create table Message(
                      id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
                      sender varchar(100) NOT NULL ,
                      recipient varchar(100) NOT NULL ,
                      topic varchar(100) NOT NULL ,
                      text_message varchar(800) NOT NULL ,
                      date TIMESTAMP

);

select * from Person;